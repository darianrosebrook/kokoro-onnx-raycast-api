# CoreML Configuration - Generated by setup_coreml.sh
# Use this to override the automatic device selection (e.g., CPUOnly, CPUAndGPU)
# COREML_DEVICE_TYPE=CPUAndGPU
export KOKORO_ORT_OPTIMIZATION=auto

# Benchmark Frequency Configuration
# Controls how often the system benchmarks hardware for optimal provider selection
# Options: daily, weekly, monthly, manually
# Default: daily
# Recommended: weekly (good balance of optimization and startup speed)
KOKORO_BENCHMARK_FREQUENCY=weekly
